// @flow
/* eslint-env browser */

import React from 'react';
import ReactDOM from 'react-dom';
import DateDisplay from './DateDisplay';

ReactDOM.render(<DateDisplay />, document.getElementById('app'));
